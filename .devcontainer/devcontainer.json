{
    "name": "Existing Dockerfile",
    "context": "..",
    "dockerFile": "./Dockerfile",
    "runArgs": [ "--gpus=all" ],
    "customizations": {
        "vscode": {
            "extensions": ["ms-python.python"]
        }
    }
}
